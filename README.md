# Casablanca

A zero-install browser-native interactive music-avatar demo. It supports typed prompts, microphone input where the browser exposes Web Speech Recognition, synthetic spoken replies, reactive portrait motion, and an original Web Audio melody.

```bash
npm run dev
```

The app is deliberately labeled as an AI demo avatar and uses an original synthetic voice rather than copying a real artist's voice.
